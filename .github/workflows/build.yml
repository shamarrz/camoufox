name: Build and Release

on:
  workflow_dispatch:
  push:
    tags:
      - "*"

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      # Fork override: only Windows x86_64. AdstackPro only ships Windows
      # and upstream's matrix has historically tripped on i686/arm builds.
      # See https://github.com/daijro/camoufox/blob/main/.github/workflows/build.yml
      # for the upstream multi-platform matrix.
      fail-fast: false
      matrix:
        target: [windows]
        arch: [x86_64]

    steps:
      - name: Maximize build space
        uses: AdityaGarg8/remove-unwanted-software@v4.1
        with:
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"
          remove-docker-images: "true"
          remove-cached-tools: "true"
          remove-swapfile: "true"
          verbose: "false"

      - name: Remove unwanted tools
        # Originally from here: https://github.com/AdityaGarg8/remove-unwanted-software/blob/master/action.yml
        run: |
          sudo apt-get remove -y '^aspnetcore-.*' > /dev/null
          sudo apt-get remove -y '^dotnet-.*' > /dev/null
          sudo apt-get remove -y '^llvm-.*' > /dev/null
          sudo apt-get remove -y 'php.*' > /dev/null
          sudo apt-get remove -y '^mongodb-.*' > /dev/null
          sudo apt-get remove -y '^mysql-.*' > /dev/null
          sudo apt-get remove -y azure-cli google-chrome-stable firefox ${POWERSHELL} mono-devel libgl1-mesa-dri --fix-missing > /dev/null
          if [[ (${CODENAME} = focal) || (${CODENAME} = jammy) ]]; then
            sudo apt-get remove -y google-cloud-sdk --fix-missing > /dev/null
            sudo apt-get remove -y google-cloud-cli --fix-missing > /dev/null
          fi
          sudo apt-get autoremove -y > /dev/null
          sudo apt-get clean > /dev/null

      - uses: actions/checkout@v2
        with:
          fetch-depth: 1

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: "1.23"

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.11"

      - name: Set up LLVM
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 18
          sudo apt-get install -y lld-18 clang-18
          if [ "${{ matrix.arch }}" != "x86_64" ]; then
            sudo apt-get install -y libc6-i386 lib32gcc-s1 lib32stdc++6 gcc-multilib g++-multilib
          fi
          sudo update-alternatives --install /usr/bin/ld.lld ld.lld /usr/bin/ld.lld-18 100

      - name: Check disk space
        run: df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          # ccache is required by Mozilla's mach configure (referenced from
          # the mozconfig); ubuntu-24.04 runners don't have it preinstalled.
          sudo apt-get install -y msitools p7zip-full aria2 ccache

      - name: Relocate heavy build dirs to /mnt (sdb1)
        run: |
          # The default runner layout puts the whole build on /dev/root
          # (~72G, only ~46G free after cleanup) while /dev/sdb1 mounted at
          # /mnt sits nearly empty (~70G free). A full Firefox compile plus
          # the 16G swapfile created below can exhaust /dev/root, which is
          # what failed run 26544570939 with "No space left on device".
          #
          # Move the multi-GB mozbootstrap toolchain (~/.mozbuild) and the
          # ccache dir onto the idle sdb1 disk so the obj dir on /dev/root
          # has the full free space to itself. The swapfile is likewise
          # created on /mnt in the "Create swap space" step.
          sudo mkdir -p /mnt/mozbuild /mnt/ccache
          sudo chown -R "$(id -u):$(id -g)" /mnt/mozbuild /mnt/ccache
          # ~/.mozbuild does not exist yet on a fresh runner; symlink it so
          # `make mozbootstrap` populates sdb1 instead of /dev/root.
          ln -sfn /mnt/mozbuild "$HOME/.mozbuild"
          echo "CCACHE_DIR=/mnt/ccache" >> "$GITHUB_ENV"
          df -h

      - name: Fetch source
        env:
          CAMOUFOX_PASSWD: ${{ secrets.CAMOUFOX_PASSWD }}
        run: |
          make fetch

      - name: Setup and bootstrap
        run: |
          # `make setup` instead of `setup-minimal` so cf_source_dir has its
          # own .git + `unpatched` tag. patch.py runs `git clean -fdx` later;
          # without an inner repo, that walks UP to the camoufox checkout's
          # .git and wipes the entire FF source tree (every file is untracked
          # from the parent's perspective). Bounding the clean to an inner
          # repo is the fix.
          # CI runners have no git identity by default; `git commit` would
          # fail. Set it ephemerally for the commit `make setup` does.
          git config --global user.email "ci@adstackpro.local"
          git config --global user.name "CI"
          make setup
          make mozbootstrap
          mkdir -p dist

      - name: Create swap space
        run: |
          # Create the swapfile on /mnt (sdb1) rather than /dev/root so the
          # 16G allocation does not eat into the disk the obj dir builds on.
          sudo fallocate -l 16G /mnt/swapfile
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h

      - name: Build
        run: python3 ./multibuild.py --target ${{ matrix.target }} --arch ${{ matrix.arch }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CamoufoxBuilds-${{ matrix.target }}-${{ matrix.arch }}
          path: dist/*

  release:
    needs: build
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: artifacts/**/*
          generate_release_notes: true
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
